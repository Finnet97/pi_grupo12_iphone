<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link width="" rel="shortcut icon" href="./images/miralo_siteLogo.png" />

	<title>Miralo - Watch Store</title>
	<!-- Font Awesome Stylesheet -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
	<!-- Bootstrap Grid Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/bootstrap-grid.min.css">
	<!-- Own Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/app.css">
</head>
<body>
	<!-- Header -->
	<% if (user) { %>
		<%- include('partials/headerLogueado') %>
	<% } else { %>
		<%- include('partials/header') %>
	<% } %>	  
	<!-- Header -->
  
	<!-- Contenido principal -->
	<main class="container">
	  <h2 class="products-title">Resultados para: "<%= busqueda %>"</h2>
  
	  <% if (productos.length === 0) { %>
		<p class="no-results">No hay resultados para su criterio de búsqueda.</p>
	  <% } else { %>
		<div class="row">
		  <% for (let i = 0; i < productos.length; i++) { %>
			<div class="col-12 col-sm-6 col-lg-3">
			  <section class="product-box">
				<a href="/product/<%= productos[i].id %>">
				  <figure class="product-box_image">
					<img src="/images/products/<%= productos[i].imagen %>" alt="<%= productos[i].nombre %>">
				  </figure>
				  <article class="product-box_data">
					<h2><%= productos[i].nombre %></h2>
					<p><%= productos[i].descripcion.substring(0, 90) %>...</p>
					<p class="product-owner">
					  Cargado por: 
					  <a href="/user/profile/<%= productos[i].usuario.id %>">
						<%= productos[i].usuario.email %>
					  </a>
					</p>
				  </article>
				</a>
			  </section>
			</div>
		  <% } %>
		</div>
	  <% } %>
	</main>
	<!-- /Contenido principal -->
  
	<!-- Footer -->
	<%- include('partials/footer') %>
	<!-- /Footer -->
</body>  
</html>